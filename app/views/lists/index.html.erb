<div class="container">

  <h1 class="my-4">Deez Leests</h1>
  <div class="row">
    <% @lists.each do |list| %>
      <div class="col-3">
        <p class="card p-5">
          <div class="image-holder"></div>
          <%= list.name %>
          <%= link_to 'Sho', list_path(list) %>
        </p>
      </div>
    <% end %>
  </div>

  <%= link_to 'New List hehe', new_list_path %>

</div>
